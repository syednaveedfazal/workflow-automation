import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["workflow-automation-s3images.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
